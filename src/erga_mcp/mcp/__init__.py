"""MCP protocol composition, contracts, policy, and transport adapters.

Domain behavior stays in :mod:`erga_mcp`; this package contains only the adapter layer that
exposes that behavior to MCP clients.
"""
